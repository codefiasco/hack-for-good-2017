<div class="innerStyle">
  <div class="container">
    <!--<h1 align="center" class="sub-title">Organizations</h1>-->
    <div align="center">
      <h1>My Applications</h1>
      <div class="input-group">
        <span class="input-group-addon button" id="basic-addon1">
            <span class="glyphicon glyphicon-search" aria-hidden="true"></span>
        </span>
        <input type="text" id="search" placeholder="Search" autocomplete="off" class="form-control center-block">
      </div>
    </div>
    <div>
    <br>
    <div class="row">
      <% @applications.each do |j| %>
            <div class="col-sm-6 col-md-4">
              <div class="thumbnail">
                <%= image_tag "profile.png", class: "list-image" %>
                <div class="caption">
                  <h3 class="text-center"><%= j.title %></h3>
                  <p><strong>Competências:</strong> <% j.skills.each_with_index do |s, idx| %><%= s.name %><%= " &#8226; ".html_safe if idx < j.skills.length - 1%><% end %></p>
                  <div class="text-center">
                    <%= link_to "Ver Detalhes", job_path(j.id), class: "btn btn-primary" %>
                  </div>
                </div>
              </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
